import 'package:flutter/material.dart';

class Notifications_page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('การแจ้งเตือน (Notifications Page)', style: TextStyle(fontSize: 24)),
    );
  }
}
